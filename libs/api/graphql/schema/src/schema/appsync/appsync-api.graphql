schema {
    query: Query
    mutation: Mutation
}

input CreateOrderFromCartInput {
  # unitId: ID!
  id: ID!
  # paymentMethod: String!
  # userLocation: ??
  # place: PlaceInput!
  # cartItems: [CartItemInput!]!
}

# input PlaceInput {
#   seat: String!
#   table: String!
# }

type Query {
  getFoo: String
}

type Mutation {
  createOrderFromCart(input: CreateOrderFromCartInput!): Boolean
    @aws_cognito_user_pools
}
