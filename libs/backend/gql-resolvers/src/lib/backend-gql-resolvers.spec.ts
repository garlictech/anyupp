import { backendGqlResolvers } from './backend-gql-resolvers';

describe('backendGqlResolvers', () => {
  it('should work', () => {
    expect(backendGqlResolvers()).toEqual('backend-gql-resolvers');
  });
});
