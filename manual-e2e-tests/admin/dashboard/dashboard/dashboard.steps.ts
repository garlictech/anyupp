import { When } from 'cypress-cucumber-preprocessor/steps';

When('I create a cash order', () => {
  /**/
});
When('I set order status to {string}', (/*status: string*/) => {
  /**/
});
When(
  'I should see {string} seat with {string} id on the floormap',
  (/*bgColor: string, seatId: string*/) => {
    /**/
  },
);
When(
  'I should see {string} border with {string} id on the floormap',
  (/*borderColor: string, seatId: string*/) => {
    /**/
  },
);
When(
  'I set order transaction status to {string}',
  (/*transactionStatus: string*/) => {
    /**/
  },
);
