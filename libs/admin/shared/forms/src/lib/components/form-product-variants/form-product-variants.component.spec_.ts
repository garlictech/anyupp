/*
import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';

import { FormProductVariantsComponent } from './form-product-variants.component';

xdescribe('FormProductVariantsComponent', (): void => {
  let component: FormProductVariantsComponent;
  let fixture: ComponentFixture<FormProductVariantsComponent>;

  beforeEach(
    waitForAsync((): void => {
      TestBed.configureTestingModule({
        declarations: [FormProductVariantsComponent],
      }).compileComponents();
    })
  );

  beforeEach((): void => {
    fixture = TestBed.createComponent(FormProductVariantsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', (): void => {
    expect(component).toBeTruthy();
  });
});
*/
