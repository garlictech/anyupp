export enum EProductType {
  DRINK = 'drink',
  FOOD = 'food',
  OTHER = 'other',
}

export enum EProductLevel {
  CHAIN = 'chain',
  GROUP = 'group',
  UNIT = 'unit',
}

export enum EProductComponentSetType {
  MODIFIER = 'modifier',
  EXTRAS = 'extras',
}
