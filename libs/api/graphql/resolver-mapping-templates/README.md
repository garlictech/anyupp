# api-graphql-resolver-mapping-templates

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test api-graphql-resolver-mapping-templates` to execute the unit tests via [Jest](https://jestjs.io).
