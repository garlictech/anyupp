export enum EProductType {
  DRINK = 'drink',
  FOOD = 'food',
  OTHER = 'other',
  TIP = 'tip',
}

export enum EProductLevel {
  CHAIN = 'chain',
  GROUP = 'group',
  UNIT = 'unit',
}

export enum EProductComponentSetType {
  MODIFIER = 'modifier',
  EXTRAS = 'extras',
}
