import { adminFirebaseConfig } from './admin-firebase-config';

describe('adminFirebaseConfig', () => {
  it('should work', () => {
    expect(adminFirebaseConfig()).toEqual('admin-firebase-config');
  });
});
