export enum EDashboardListMode {
  current = 'current',
  history = 'history',
  lanes = 'lanes',
  floormap = 'floorMap',
  reports = 'reports',
}

export enum EDashboardTicketListType {
  placed = 'placed',
  manual = 'ready',
  problematic = 'paymentIntention',
}
